Getting Started
To start the application, follow these simple steps:

1. Navigate to the Main Directory: Open your command prompt or terminal and navigate to the main directory of the application.

2. Install Flask (if not already installed): If Flask is not installed on your system, you can install it using pip:
3. Execute the Python Script: Once you're in the main directory, simply type the following command and press Enter:

"python app.py"

Enjoy: Once the script is executed, the application will start running.